#functions: define and reuse

"""
define and invoce
"""

def (hello)
	print ('Hi')
	print ('Bye')

def (hello)
print ('Back again?')
def (hello)

#Build in: float(), type(), int(), raw_input(), max(), min()...
"""
function(agument) >> returns value and is being assigned. (input>output)

eg.

big = max('world')
print ('big')
>w
"""
